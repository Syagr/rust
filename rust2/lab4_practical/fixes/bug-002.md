# Fix for bug-002

**Issue**: JsonStore fails if the parent directory for the JSON index file does not exist.

**Fix**
- Create parent directories before writing the JSON index file.

**Link to code**
- Commit or PR: <add link after you push>
