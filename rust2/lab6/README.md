Практична робота 6 — `rust2/lab6`

Файли:
- `PRAKTICHNA_ROBOTA_6.md` — звіт та відповіді (українською).
- `EXAMPLES.md` — приклади коду, що ілюструють помилки через `!Send`/`!Sync`.

Швидкі інструкції для гіта:

```bash
cd C:/It/projects/rust
git checkout -b praktika6
git add rust2/lab6/PRAKTICHNA_ROBOTA_6.md rust2/lab6/EXAMPLES.md rust2/lab6/README.md
git commit -m "Add practical work 6 (Send/Sync)"
git push origin praktika6
```

Якщо потрібно — підготуємо cargo-проєкт з прикладами або PDF формат звіту.